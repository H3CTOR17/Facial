<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Facial" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/celeste_medio</item>
        <item name="colorPrimaryVariant">@color/celeste_medio</item>
        <item name="colorOnPrimary">@color/celeste_medio</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <style name="Theme.Facial" parent="Base.Theme.Facial" />

<!--    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenBackground">@color/celeste_medio</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/LOGO</item>
        <item name="postSplashScreenTheme">@style/Theme.Facial</item>
    </style>-->
</resources>
